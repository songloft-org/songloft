{
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "sync": "node ../scripts/sync-docs.mjs",
    "fetch-issues": "node ../scripts/fetch-issues.mjs",
    "docs:dev": "npm run sync && npm run fetch-issues && vitepress dev --host=0.0.0.0 --port=3030",
    "docs:build": "npm run sync && npm run fetch-issues && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "markdown-it-task-lists": "^2.1.1",
    "vite-plugin-vitepress-auto-sidebar": "^1.7.0"
  }
}
